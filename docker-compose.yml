services:
  api:
    build:
      target: builder
      context: interface
    container_name: omnidialer-api
    volumes:
      - ./interface:/app
    ports:
      - '1440:1440'
    networks:
      - network1
    stop_signal: SIGINT

networks:
  network1:
    name: devenv_omnileads
    external: true
