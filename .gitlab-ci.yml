stages:
  - build

build-docker:
  stage: build
  services:
    - name: docker:dind
      entrypoint: ["env", "-u", "DOCKER_HOST"]
      command: ["dockerd-entrypoint.sh"]
  variables:
    DOCKER_HOST: tcp://docker:2375/
    DOCKER_DRIVER: overlay2
    DOCKER_TLS_CERTDIR: ""
    INFRA: docker
  image: gitlab/dind
  script:
    - cd build/docker && bash build_images.sh
  only:
    - web
    - /.*release.*$/
    - /.*master.*$/

build-rpm:
  stage: build
  variables:
    INFRA: onpremise
    ENV: prodenv
  image: freetechsolutions/fpm-ansible:centos7
  script:
    - cd build/rpm && bash build_rpm.sh
  only:
    - web
    - /.*release.*$/
    - /.*master.*$/
