# inspect the values of the campaign
#+begin_src sql :engine postgresql :dbhost localhost :dbuser omnidialer :dbpassword dialer456 :database omnidialer :dbport 5433
select * from only campaign where id = 4;
#+end_src

# for take a look at the statuses of the contacts
#+begin_src sql :engine postgresql :dbhost localhost :dbuser omnidialer :dbpassword dialer456 :database omnidialer :dbport 5433
select id, status, history from only contact_in_campaign where id_campaign = 4;
#+end_src

# inspect the incidence rules for the campaign
#+begin_src sql :engine postgresql :dbhost localhost :dbuser omnidialer :dbpassword dialer456 :database omnidialer :dbport 5433
select * from only incidence_rules where campaign_id = 4;
#+end_src

# insert an incidence_rule for congestion
#+begin_src sql :engine postgresql :dbhost localhost :dbuser omnidialer :dbpassword dialer456 :database omnidialer :dbport 5433
insert into incidence_rules values (2, 4, 'congestion', 3, 40, 1, 4)
#+end_src

# insert an incidence_rule for busy
#+begin_src sql :engine postgresql :dbhost localhost :dbuser omnidialer :dbpassword dialer456 :database omnidialer :dbport 5433
insert into incidence_rules values (1, 1, 'busy', 4, 20, 1, 4)
#+end_src

# Cleaning the contact statuses in the campaign, just for start fresh, useful for see again the calls behavior without
# restart anything
#+begin_src sql :engine postgresql :dbhost localhost :dbuser omnidialer :dbpassword dialer456 :database omnidialer :dbport 5433
update contact_in_campaign set status = 2, history = '{}';
#+end_src

# setting only 2 contacts that will lead to busy and congestion so we can test incidence rules
#+begin_src sql :engine postgresql :dbhost localhost :dbuser omnidialer :dbpassword dialer456 :database omnidialer :dbport 5433
update contact_in_campaign set status = 2, history = '{}' where id_campaign = 4 and (id = 106 or id = 193);
#+end_src




# Use this following query, or a similar to establish opening hours for the campaign
#+begin_src sql :engine postgresql :dbhost localhost :dbuser omnidialer :dbpassword dialer456 :database omnidialer :dbport 5433
+update campaign set hour_start = '00:00', hour_ends = '23:59', tuesday = True, end_date = '2024-10-29' where id = 4;
#+end_src
